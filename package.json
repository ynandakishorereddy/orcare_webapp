{
  "name": "orcare-html",
  "version": "1.0.0",
  "description": "ORCare Web — plain HTML/CSS/JS build",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
